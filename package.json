{
  "private": true,
  "workspaces": [
    "frontend",
    "packages/gitlab-client",
    "services/partinfo",
    "services/board-files"
  ],
  "scripts": {
    "nginx-conf": "./nginx/make_conf" ,
    "docker": "docker-compose up",
    "start:frontend": "cd frontend && yarn start",
    "start": "yarn run nginx-conf && yarn run docker & yarn run start:frontend"
  }
}
