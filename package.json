{
  "private": true,
  "workspaces": [
    "frontend",
    "packages/gitlab-client",
    "services/partinfo",
    "services/board-files"
  ],
  "scripts": {
    "nginx": "./nginx/make_conf" ,
    "docker": "docker-compose up",
    "start:frontend": "cd frontend && yarn start",
    "start": "yarn run nginx && yarn run docker > /dev/null & yarn start:frontend"
  }
}
