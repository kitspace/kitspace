{
  "name": "pcbhub",
  "version": "1.0.0",
  "description": "",
  "main": "\"\"",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    , "make": "mkdir -p build && elm-make src/*.elm --yes --output build/"
  },
  "author": "",
  "license": "AGPLv3",
  "devDependencies": {
    "elm": "^1.4.1",
    "gulp-elm": "^0.2.0"
  }
}
