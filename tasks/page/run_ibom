#!/bin/bash
set -euo pipefail
dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# Start Xvfb
Xvfb :99 -ac -nolisten tcp &
xvfb=$!

export DISPLAY=:99

python3 "${dir}/run_ibom.py" "$1" "$2" "$3"

kill -15 $xvfb
